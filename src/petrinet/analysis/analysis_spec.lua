-- local assert   = require "luassert"
-- local Analysis = require "petrinet.analysis"
-- local petrinet = require "petrinet.example"

describe ("Analysis", function ()

  -- it ("can perform an analysis of the sport Petri net", function ()
  --   local analysis = Analysis {
  --     petrinet = petrinet,
  --   }
  --   for result in analysis () do
  --     local count = 0
  --     for _ in pairs (result.deadlocks) do
  --       count = count + 1
  --     end
  --     print (result.free, #result.states, result.choice_ratio, count)
  --   end
  -- end)

end)
